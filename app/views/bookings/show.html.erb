
 <div class="header-container">
     <div class="banner">
    <%= image_tag 'horse.jpg', class: 'img-banner' %>
  </div>
    </div>
    <div class="header-text">
      <h1 class="bookings-header-h1">Yay! You and <%= @animal.name %> are going on an adventure together!</h1>
      <h5 class="bookings-header-h5">Your booking has been successful. Now put down the dates in your calendar. <%= @animal.name %> does not like to wait for humans.</h5>

      <ul class="list-styling">
        <li>Start Date: <%= @booking.date_start %></li>
        <li>End Date: <%= @booking.date_end %></li>
        <li>Total Price: <%= @booking.total_price %>€</li>
        <li>Pick Up and Drop Off: <%= @animal.user.street %> <%= @animal.user.house_number %> </li>
        <li>Owner Name: <%= @animal.user.first_name %> <%= @animal.user.last_name %></li>
      </ul>

  <%= link_to "Book more fun experiences with animals", root_path, class: "button", id: "btn-back" %>
    </div>
  </div>


</div>
