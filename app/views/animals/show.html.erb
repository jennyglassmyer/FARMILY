<div class="show-header">
  <h1 class="show-header-h1"> <%= link_to"〈", root_path, class: "arrow" %> Meet <%= @animal.name %></h1>
</div>

<div class="container" id="show-container">


  <div class="show-img">
    <% @animal.photos.each do |photo| %>
      <%= cl_image_tag photo.key, height: 350, width: 500, crop: :fill, radius: 10; %>
    <% end %>
    <div class="map-box">
      <div id="map"
        style="width: 100%; height: 300px;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>
  </div>

  <div class="show-profile">
    <p>Owner: <%= @animal.user.first_name %> <%= @animal.user.last_name %></p>
    <p>Species: <%= @animal.species %></p>

    <% if @animal.breed == "" %>
    <% else %>
      <p>Breed: <%= @animal.breed %></p>
    <% end %>

    <p>Gender: <%= @animal.gender %></p>
    <p>Personality: <%= @animal.personality %></p>
    <p>Description: <%= @animal.description %></p>
    <p>Care level: <%= @animal.care_level %></p>
    <p>Age: <%= @animal.age %></p>
    <p>Price per day: <%= @animal.price_per_day %>€</p>
    <p>Care instructions: <%= @animal.care_instructions %></p>

  <h2>Create Booking for <%= @animal.name %></h2>

    <div class="date-booking">
      <%= simple_form_for [@animal, @booking] do |f| %>
      <%= f.input :date_start, label: "Date of Pickup" %>
      <%= f.input :date_end, label: "Date of Drop Off" %>
      <%= f.submit :class => "button" %>
      <% end %>
    </div>

  </div>
</div>
